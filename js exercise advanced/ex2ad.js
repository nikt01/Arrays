const originalArray = [1, 5, 10, 15, 20, 25];
const a = 10;
const b = 20;

const filteredArray = filterRange(originalArray, a, b);

console.log("Original Array:", originalArray);

console.log(`Filtered Array (${a} to ${b}):`, filteredArray);
